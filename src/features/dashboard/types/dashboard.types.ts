export interface Stat {
    count: number;
    label: string;
    color: string;
}

export interface StatData {
    count: number;
    label: string;
    fill: string;
}

export interface StatDate {
    count: number;
    year: number;
    mounth: string;
}